name: Node.js CI

on:
  push:
    branches:
      - main

jobs:
  builds:
    runs-on: ubuntu-20.04

    strategy:
      matrix:
        node-version: [15.x]

    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
      - run: npm install
      - run: npm test
        env:
            AUTH_HASH: ${{ secrets.AUTH_HASH }}
            AUTH_KEY: ${{ secrets.AUTH_KEY }}
            AUTH_MAX_AGE: ${{ secrets.AUTH_MAX_AGE }}
            DB_DB: ${{ secrets.DB_DB }}
            DB_HOST: ${{ secrets.DB_HOST }}
            DB_PASS: ${{ secrets.DB_PASS }}
            DB_PORT: ${{ secrets.DB_PORT }}
            DB_USER: ${{ secrets.DB_USER }}
            LIMITER_DELAY: ${{ secrets.LIMITER_DELAY }}
            LIMITER_MAX: ${{ secrets.LIMITER_MAX }}
            LIMITER_RETRY: ${{ secrets.LIMITER_RETRY }}
            SERVER_PORT: ${{ secrets.SERVER_PORT }}
      - run: npm run build
      - run: echo $DEPLOY_KEY >> key
        env:
          DEPLOY_KEY: ${{ secrets.DEPLOY_KEY }}
      - run: scp -i "key" -P $DEPLOY_PORT $DEPLOY_USER@$DEPLOY_HOST dist/wkblog.js $DEPLOY_TARGET
        env:
          DEPLOY_PORT: ${{ secrets.DEPLOY_PORT }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_TARGET: ${{ secrets.DEPLOY_TARGET }}
        